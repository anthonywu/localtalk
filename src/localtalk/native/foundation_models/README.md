# localtalk-fm

Swift CLI bridging LocalTalk to Apple's Foundation Models framework (`SystemLanguageModel`).

Built on first use into `~/.cache/localtalk/bin/localtalk-fm` via `swiftc` (Xcode CLT).

Requires macOS 27.0+ with Apple Intelligence / Foundation Models available.
